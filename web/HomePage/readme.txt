* Launch Server in jojogo/server at localhost:8081 by running "go run main.go"
* In jojogo/web/HomePage, "run apt install npm"
* And then run "npm install -g local-web-server"
* And then run "ws"
* Then by typing "http://localhost:8000/" in browser the webpage should be shown
